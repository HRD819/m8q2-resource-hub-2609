# 交接記錄檔

## 2026-09-04 13:16 +08:00 — MVP 架構、Demo 整合與本機驗證

### 已完成

- 依 `AGENTS.md` 完整檢查 `SPEC.md`、README、實作計畫、決策與全部初始內容。
- Audit 使用者放入的 `accessible_course_demo_v2/`。保留其簡潔版面、skip link、清楚 focus、固定標題順序與六個示範教材的意圖；Demo HTML 本身不部署。
- 建立零第三方相依的 Node.js 靜態產生器與 JSON content schema。
- 建立首頁、2 個課程頁、6 個獨立單元頁，共 9 個 production HTML。
- 六個單元都有明確標示「不是正式教材」的 Dropbox 示範項目；沒有把教材檔案複製進 repository。
- 加入 schema／Dropbox URL 驗證、Node tests、HTML／link／heading／landmark／noindex／focus／contrast／responsive 規則檢查。
- 建立 GitHub Actions 品質 gate 與 GitHub Pages workflow。Push 預設只檢查；人工觸發或 `PUBLISH_PAGES=true` 才發布。
- 補齊 README、content 維護說明與 `ACCESSIBILITY_TESTING.md`。

### 決策與排除

- 使用 Node.js 標準庫而非 SPA、CMS、資料庫或大型 framework；見 `DECISIONS.md` ADR-008。
- 使用 JSON 作為內容單一來源，`dist/` 僅為產出物，不得手動編輯。
- 全部站內連結使用相對路徑，支援 GitHub Pages project-site base path。
- 未新增登入、密碼、robots.txt、sitemap、feed、前端 JavaScript 或教材檔案副本。
- 目前仍為示範內容且缺少真人 accessibility 驗收，因此以 ADR-010 防止 push 意外公開。

### 驗證證據

- `npm run ci`：內容驗證通過（2 門課、6 個單元、6 項教材）。
- Node tests：4/4 通過，涵蓋單元結構、Dropbox HTTPS、HTML escaping 與拒絕非 Dropbox 教材 URL。
- Production build：9 個 HTML 頁面。
- 產出頁檢查：noindex、landmarks、標題、內部連結、focus、主要色彩 4.5:1 對比、responsive media query、無 sitemap/feed 均通過。
- `computer-use` 因專案路徑方括號觸發其 sandbox glob 錯誤；Chrome headless GPU 初始化失敗，Edge headless 未產生截圖。因此未宣稱完成視覺、keyboard 或 NVDA 驗收。
- Headless QA 建立的臨時 Edge profile 已移除，沒有停止使用者的 Edge 程序。
- 本機 Git：`main`、`origin` 與初始提交 `cdce7d8` 已存在；目前未確認遠端 `main`、repository 可見性、Pages 設定或 workflow 執行結果。

### 優先待辦

1. 取得正式單元說明、教材 metadata 與 Dropbox 分享連結，逐項替換六個示範項目。
2. 依 `ACCESSIBILITY_TESTING.md` 完成 keyboard-only、NVDA + Chrome 與 320／390／768／1440 px 人工驗收。
3. 對正式頁面執行 axe、Lighthouse Accessibility 與 HTML validator。
4. 確認 GitHub repository 可見性符合需求、遠端 `main` 與 Pages Source 設定。
5. 第一次發布應人工執行 workflow；通過後再決定是否設定 `PUBLISH_PAGES=true`。

## 2026-09-04 14:30 +08:00 — 資訊組織 1-00 至 1-06 內容架構

### 已更新

- 資訊組織單元改用教師指定編碼顯示：`1-00` 至 `1-06`；穩定網址仍保留 `unit-00/` 至 `unit-06/`。
- `1-00 課程介紹` 以圖片中的精確檔名登錄 4 筆預計教材：
  - `1-00.1151資訊組織課程大綱_發放用.docx`
  - `1-00.1151資訊組織課程大綱_發放用.pdf`
  - `1-00.資訊組織課程簡介260904.pdf`
  - `1-00.資訊組織課程簡介260904.pptx`
- 建立 `1-01 資訊組織概論`、`1-02 編目規範`、`1-03 標準號碼`、`1-04 卡片目錄`、`1-05 中國編目規則`、`1-06 英美編目規則`。
- 依使用者說明，每個參考檔案先登錄 PowerPoint（PPTX）與 PDF 兩種格式。中國編目規則依圖片分為總則、圖書、連續性資源、電子資源／分析、乙編標目五組。
- 資訊組織目前共 24 筆 `planned` 教材；取得正式檔名與 Dropbox 分享連結後再轉為 `available`。

### Schema 與呈現決策

- 單元新增可選 `code` 欄位，畫面顯示 `code + title`，數字 `unit` 繼續負責排序與穩定 URL。
- 教材新增 `status: planned` 與可選 `filename`。Planned 教材不允許設定 `dropbox_url`，頁面顯示「Dropbox 連結尚未提供」且不產生假連結。
- 作業、線上測驗、中國編目規則下載網站與英美編目規則教材頁目前沒有完整 URL／維護欄位，未混入教材下載清單；日後可依需求另建活動或補充資源資料。
- 參考資源課程尚未提供教師編碼，維持原顯示方式。

### 驗證

- `npm run ci` 通過：2 門課、10 個單元、27 項教材；4/4 tests 通過。
- Production build 產生 13 個 HTML 頁面；noindex、landmarks、標題、連結、focus 與 responsive 規則通過。
- 抽查資訊組織課程頁，確認依序顯示 `1-00` 至 `1-06`。
- 抽查 `1-00`，四個原始檔名均正確顯示，且 HTML 不含 planned 教材的 Dropbox 假連結。

### 待補

1. 各單元正式文字說明。
2. `1-01` 至 `1-06` 的精確 PPTX／PDF 檔名。
3. 全部正式 Dropbox 分享連結與更新日期。
4. 如需在本站呈現作業、測驗、指定教材網站與繳交期限，先確認活動資料模型與 URL。

## 2026-09-04 16:09 +08:00 — 教科書段落與資訊組織個別下載連結

### 已更新

- 在 `1151 資訊組織` 課程頁新增「教科書」段落，共 3 筆：
  - 張慧銖等（2017）《資訊組織》：輔大圖書館電子書與公博樓索書號。
  - 《中國編目規則》第三版：教師提供的 Dropbox PDF 個別下載連結。
  - Mortimer（2007）*Learn descriptive cataloging*：輔大圖書館電子書。
- 每筆教科書均保留教師提供的使用時機、紙本建議與版本提醒；連結文字直接說明「開啟電子書」或「下載 PDF」。
- 從教師提供的 Dropbox 公開資料夾渲染頁核對實際檔名與獨立檔案網址，將 `1-00` 至 `1-05` 共 22 筆 DOCX／PDF／PPTX 教材由 `planned` 轉為可下載。
- 全部正式教材網址改用檔案層級連結及 `dl=1`，網頁連結文字改為「下載：教材名稱（格式）」。沒有把教材檔案複製進 repository。
- `1-05_01` 的《S.》中文版開箱文網址捷徑檔已加入補充資料，亦使用個別 Dropbox 直接下載網址。
- 新增可選的 course-level `textbooks` schema、內容驗證、語意化 ordered list 呈現與維護說明。

### 驗證與範圍

- Dropbox 公開資料夾以無登入、臨時 Chrome profile 讀取；擷取到的每筆教材皆有獨立檔案 ID 與精確檔名，臨時 profile 已安全移除。
- `npm run validate` 通過：2 門課程、10 個單元、27 項教材。
- Node tests 4/4 通過；確認資訊組織 22 筆正式教材皆有 `dl=1` 個別網址、2 筆未上傳教材維持 `planned`。
- Production build 產生 13 個 HTML 頁面；`npm run check` 確認 noindex、landmarks、標題、連結、focus 與 responsive 規則通過。
- 抽查課程頁：教科書為獨立 `h2` 區段，三個連結文字具體；抽查 `1-05`：十個教材下載連結與一個補充下載連結均已輸出。

### 待補

1. Dropbox 公開資料夾目前未出現 `1-06 英美編目規則` 的 PPTX／PDF，因此保留 2 筆 `planned`，未建立猜測連結。
2. 各單元正式文字說明仍待提供。
3. 正式發布前仍需完成 keyboard-only、NVDA + Chrome、多寬度視覺、axe／Lighthouse 與 HTML validator 人工／外部驗收。

## 2026-09-04 16:20 +08:00 — 課程頁首用途與 TronClass 聲明

### 已更新

- 在 `content/site.json` 新增全站 `course_notice`，內容為教師指定的用途限制與正式資訊來源聲明。
- 每一門課程頁在麵包屑後、課程標題前顯示聲明，並以具體連結文字提供輔大 TronClass 網址：`https://elearn2.fju.edu.tw/`。
- 聲明採全站設定而非硬編碼課程 slug，未來新增課程頁會自動套用；單元頁不重複顯示，避免干擾單元教材導覽。
- 新增 `course_notice` 的 HTTPS 與必要欄位驗證，並補充 README 維護說明。

### 驗證

- `npm run ci` 通過：內容驗證、4/4 tests、13 頁建置與網站檢查均成功。
- 產出頁抽查確認課程頁存在 `課程頁面使用聲明` 與 TronClass 連結；兩門課共用同一份聲明資料。

### 待補

- 仍待依 `ACCESSIBILITY_TESTING.md` 完成正式站 keyboard-only、NVDA + Chrome 與外部 validator／Lighthouse 驗收。

## 2026-09-04 16:29 +08:00 — 參考資源 1-00 課程介紹與作業一

### 已更新

- 參考資源第 0 單元改用教師指定編碼與標題：`1-00 課程介紹`。
- 依截圖登錄四個課程介紹檔案名稱：`1-00.1151參考資源課程大綱_發放用.docx`、同名 PDF、`1-00.參考資源課程簡介260904.pdf` 與同名 PPTX。
- 因本次未提供這四個檔案的個別 Dropbox 分享網址，四筆均維持 `planned`，不產生猜測或無效下載連結。
- 在 `1-00` 新增「作業與學習活動」區段與「作業一」：包含 Google 文件題目網址、11 月 13 日上課前期限、紙本及電子檔繳交要求，以及四項作業規則。
- 新增可選的 `activities` 單元欄位、HTTPS／必要欄位驗證、語意化作業區段呈現與 README 維護流程。

### 驗證

- `npm run ci` 通過：內容驗證、4/4 tests、13 頁建置與網站檢查均成功。
- 產出頁抽查確認 `1-00 課程介紹`、四個檔名、作業一題目 Google 文件連結、期限、繳交方式與規則均已輸出。
- 教師提供的 Google 文件網址僅保存為活動連結；網站不複製或代管作業題目內容。

### 待補

1. 四個參考資源 `1-00` 檔案的個別 Dropbox 下載網址。
2. 作業題目後續新增內容仍由 Google 文件維護；若要在本站同步摘要，需另確認更新流程。

## 2026-09-04 17:05 +08:00 — 參考資源 1-01 至 1-08 單元架構

### 已更新

- 參考資源課程單元由 `1-00`～`1-02` 擴充至 `1-00`～`1-08`，並統一加入課程內顯示編碼。
- 依附圖更新單元名稱：
  - `1-01` 參考資源概論及種類 Ch. 1-2
  - `1-02` 參考資源利用與選擇 Ch. 3-4
  - `1-03` 書目資源 Ch. 5、8
  - `1-04` 網路資源與搜尋引擎
  - `1-05` 新聞資訊 Ch. 9-4
  - `1-06` 媒體與資訊識讀
  - `1-07` 字辭典、百科全書 Ch. 6
  - `1-08` 人物資訊 Ch. 10
- 每個 `1-01`～`1-08` 單元先建立 PowerPoint 與 PDF 兩筆 `planned` 教材；尚未取得檔名或個別 Dropbox 連結，不建立假下載連結。
- `1-03`～`1-08` 預留 `activities` 陣列並加入 `activities_note`，支援每單元後續加入多個線上作業；目前只輸入已提供完整資料的 `1-00 作業一`。
- 移除參考資源 `1-01`、`1-02` 的示範教材，避免附圖所述正式架構仍顯示示範項目。

### 驗證

- `npm run ci` 通過：2 門課程、16 個單元、44 項教材；4/4 tests、19 頁建置與網站檢查均成功。
- 產出課程頁抽查確認參考資源依序顯示 `1-00` 至 `1-08`；各單元教材清單均有 PPTX／PDF 待提供項目。

### 待補

1. `1-01`～`1-08` 各 PPTX／PDF 的實際檔名與個別 Dropbox 下載網址。
2. `1-03` 起各單元的線上作業題目、網址、期限與繳交規則。

## 2026-09-04 — 1-00 單元說明更新後重新部署

### 已完成

- 將兩門課 `1-00` 的結構化單元說明與校方資源連結提交為 `83b14d1`，推送至 GitHub `main`。
- GitHub Actions workflow run `33885685936` 的品質檢查與 GitHub Pages 部署 jobs 均成功。
- Pages deployment 已對應 `83b14d19d0ab9a26fa0109f8e2bc7d6d5b739aa9`；正式網址仍為 <https://hrd819.github.io/m8q2-resource-hub-2609/>。

### 尚待確認

- 仍需在一般網路環境開啟正式網址，確認頁面內容、Dropbox 下載與 NVDA／人工無障礙流程。

## 2026-09-04 22:24 +08:00 — 課程頁與教科書導覽調整

### 已更新

- 教科書改為與課程單元同層級的課程內容卡片；課程頁只顯示教科書名稱，點入 `textbook-<slug>/` 獨立頁後才顯示書目、電子版連結、索書號與使用說明。
- 新增教科書 `title`／`slug` 欄位與驗證，並將教科書獨立頁納入穩定 URL、頁數與網站檢查；建置頁數由 19 頁增加為 23 頁。
- 兩門課均新增課程頁首補充說明：「1-01單元以後的課程內容都有在TronClass提供pdf檔,此處額外提供可編輯的檔案，因此一樣的檔案會有2種檔案格式」。
- 全站課程頁面開頭聲明濃縮為教師指定版本；保留 TronClass 網址連結。
- 移除所有單元描述中的「正式文字說明待補／待教師補充」暫代文字，保留實際可用的單元簡介。

### 驗證

- `npm run ci` 通過：內容驗證、4/4 測試、23 頁建置與網站檢查成功。
- 抽查兩門課程頁確認教科書只呈現為可點入項目；抽查教科書獨立頁確認詳細內容正常顯示。
- 搜尋來源與 `dist`，未再發現上述待補暫代文字。

### 待補

- 本次修改尚未重新部署 GitHub Pages；待教師確認版面與文字後，再提交並發布。

## 2026-09-04 22:28 +08:00 — 參考資源作業一內容精簡

### 已更新

- `1151 參考資源` `1-00` 的作業一改為精簡內容：類型為「Google文件檔(唯讀)」、補充 Google 文件下載並以 Word 編輯的操作說明、保留題目文件連結，以及指定完成期限與 TronClass 上傳方式。
- 移除原先四項「作業要求」清單；活動 schema 現在允許沒有額外 `instructions` 的精簡活動。
- 同步更新 README 維護說明與內容測試。

### 驗證

- `npm run ci` 通過：內容驗證、4/4 測試、23 頁建置與網站檢查成功。
- 產出頁抽查確認只顯示指定的作業一精簡內容，沒有舊的作業要求清單。

### 待補

- 本次修改尚未重新部署 GitHub Pages；待教師確認後再發布。

## 2026-09-04 20:36 +08:00 — GitHub Pages 首次發布

### 已完成

- 依 `GitHub_Pages_與現有網頁整合交接.md` 流程，建立發布提交 `04687d2`（`Publish accessible course materials v1`）並推送至 GitHub `main`。
- Actions run `33873532088` 的品質檢查與「部署 GitHub Pages」jobs 均成功。
- Pages 正式網址為 <https://hrd819.github.io/m8q2-resource-hub-2609/>，Pages API 回報 HTTPS 已啟用。
- 本次採一次性 `workflow_dispatch` 發布，未啟用 `PUBLISH_PAGES=true` 自動部署。

### 尚待確認

- 目前工作環境無法解析 `github.io` DNS，尚未能從此環境以 HTTP 開啟正式網址；需在一般網路環境確認首頁、課程頁、單元頁、noindex 與 Dropbox 連結。
- `ACCESSIBILITY_TESTING.md` 所列 Windows + Chrome + NVDA、axe／Lighthouse、HTML validator 與人工視覺驗收仍待完成。

## 2026-09-04 16:44 +08:00 — 發布前驗收（第一版）

### 已驗證

- `npm run ci` 通過：內容驗證、4/4 測試、19 頁建置與網站檢查均成功。
- 本地預覽伺服器實際請求首頁、兩門課程頁與全部 16 個單元穩定網址，共 19 個 URL，全部回傳 HTTP 200。
- 生成的 19 個 HTML 頁面均含 `noindex, nofollow`、skip link、`main` landmark；未發現空白連結，並確認 32 個 Dropbox `dl=1` 下載網址已輸出。
- 以無頭 Chrome 產生 320、390、768、1440 CSS pixel 寬度的參考資源單元頁截圖；截圖產生成功，但目前影像檢視工具無法讀取該工作目錄檔案，因此未宣稱完成人工視覺通過。

### 限制與待辦

1. 以 PowerShell HEAD／GET 驗證 Dropbox 時，環境目前無法解析 `www.dropbox.com`／`dropboxusercontent.com` DNS；因此遠端檔案權限與下載回應需在一般網路環境再確認。
2. 尚未完成 Windows + Chrome + NVDA 的人工鍵盤流程、axe／Lighthouse／HTML validator；發布前應依 `ACCESSIBILITY_TESTING.md` 完成並記錄版本與結果。
3. GitHub Pages 尚未部署，正式網址與 Pages base path 尚未驗證。
3. 各單元正式文字說明。

## 2026-09-04 16:35 +08:00 — 參考資源教科書與 TronClass 講義說明

### 已更新

- 在 `1151 參考資源` 課程頁新增教科書：張淳淳等編著（2003）《參考資源與服務》，並加入輔大圖書館電子書連結。
- 在教科書段落後新增課程教材說明：「其餘使用的教材則為老師自編的講義，可在 TronClass 下載。」
- 同一說明保留在教科書項目的使用說明中，讓學生使用螢幕閱讀器瀏覽時也能在書目附近理解教材取得方式。
- 新增可選的 course-level `materials_note` 欄位、必要字串驗證、課程頁呈現與 README 維護說明。

### 驗證

- `npm run ci` 通過：內容驗證、4/4 tests、13 頁建置與網站檢查均成功。
- 產出頁抽查確認 `1151 參考資源` 顯示教科書、電子書連結與 TronClass 講義說明；全站用途聲明仍保留在兩門課頁首。

## 2026-09-04 16:38 +08:00 — 參考資源 Dropbox 個別下載連結

### 已更新

- 讀取教師提供的參考資源 Dropbox 資料夾，確認目前共有 8 個檔案：`1-00` 課程介紹 4 個、`1-01` 2 個、`1-02` 2 個。
- 將上述 8 筆教材在 `content/courses/1151-reference-resources/unit-00.json`～`unit-02.json` 標記為可用，保留實際檔名，並寫入各自的 Dropbox 個別網址。
- 個別網址統一使用 `dl=1`，因此網站上的「下載」連結會直接觸發檔案下載；未把教學檔案複製到網站專案。
- `1-03`～`1-08` 因新資料夾目前沒有對應檔案，仍維持 `planned`，不建立猜測連結。

### 驗證

- 更新內容測試，確認全站仍有 44 筆教材、其中 8 筆為本次新增的參考資源可用教材，未提供檔案的教材維持 14 筆 `planned`。
- 測試會檢查 8 筆新連結均為 Dropbox HTTPS 網址、含 `dl=1` 且具有檔名；完成建置後再抽查輸出頁面的下載文字與網址。

### 待補

1. `1-03`～`1-08` 各單元 PPTX／PDF 的實際檔案與個別 Dropbox 下載網址。
2. `1-03` 起各單元的線上作業題目、網址、期限與繳交規則。
## 2026-09-04 23:00 +08:00 — 課程卡片、首頁與補充資源顯示調整

### 已更新

- 首頁名稱改為「董老師課程教材分享平台」，用途濃縮為「依課程與單元查找教材取得檔案。」；新增平台用途聲明與 TronClass 網址。
- 移除全站 footer 原有的 Dropbox 唯一來源說明文字，保留 footer landmark。
- 兩門課程總覽的單元卡片移除單元介紹文字，教材數量改為只顯示可開啟數量，例如「4 項教材資料可開啟。」或「0 項教材資料可開啟。」。
- 資訊組織三本教科書與參考資源教科書卡片加入指定搭配單元／使用範圍文字；`Learn descriptive cataloging` 卡片只保留「搭配單元1-06使用」。
- 資訊組織 `1-05` 的《S.》補充資料由 Dropbox `.url` 捷徑改為直接連到 Booklife 網頁。
- 除兩門課的 `1-00` 課程介紹外，其他單元頁不再輸出「單元說明」區段；`1-00` 的課程說明與有序清單維持不變。
- 新增教科書卡片欄位驗證與首頁聲明 schema 說明，並同步更新 README、內容說明與測試。

### 驗證

- `npm run ci` 通過：內容驗證、4/4 測試、23 頁建置與網站檢查成功。
- 已抽查建置產出：首頁新名稱／用途／TronClass 聲明、兩門課卡片可開啟數量、Booklife 連結、1-00 與非 1-00 單元說明區段，以及舊 footer 文字均符合指定結果。
- `git diff --check` 通過；`dist/` 僅為建置產出，未作為內容來源修改。

### 待補

- 本批修改尚未重新部署 GitHub Pages；待教師確認後再提交並發布。

## 2026-09-04 23:42 +08:00 — 重新部署 GitHub Pages

### 已完成

- 將本批修改提交為 `3f83e2c`（`Update course cards and homepage content`）並推送至 GitHub `main`。
- 手動觸發 GitHub Actions workflow run `33890496189`；「品質檢查」與「部署 GitHub Pages」jobs 均成功。
- 正式網址維持為 <https://hrd819.github.io/m8q2-resource-hub-2609/>，Pages API 確認 HTTPS 已啟用。

### 驗證

- 以正式網址請求首頁回應 HTTP 200。
- 正式首頁標題為「董老師課程教材分享平台」，且包含新的平台聲明與 `noindex, nofollow`。
