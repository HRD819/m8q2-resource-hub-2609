# 交接記錄檔

## 2026-09-04 13:16 +08:00 — MVP 架構、Demo 整合與本機驗證

### 已完成

- 依 `AGENTS.md` 完整檢查 `SPEC.md`、README、實作計畫、決策與全部初始內容。
- Audit 使用者放入的 `accessible_course_demo_v2/`。保留其簡潔版面、skip link、清楚 focus、固定標題順序與六個示範教材的意圖；Demo HTML 本身不部署。
- 建立零第三方相依的 Node.js 靜態產生器與 JSON content schema。
- 建立首頁、2 個課程頁、6 個獨立單元頁，共 9 個 production HTML。
- 六個單元都有明確標示「不是正式教材」的 Dropbox 示範項目；沒有把教材檔案複製進 repository。
- 加入 schema／Dropbox URL 驗證、Node tests、HTML／link／heading／landmark／noindex／focus／contrast／responsive 規則檢查。
- 建立 GitHub Actions 品質 gate 與 GitHub Pages workflow。Push 預設只檢查；人工觸發或 `PUBLISH_PAGES=true` 才發布。
- 補齊 README、content 維護說明與 `ACCESSIBILITY_TESTING.md`。

### 決策與排除

- 使用 Node.js 標準庫而非 SPA、CMS、資料庫或大型 framework；見 `DECISIONS.md` ADR-008。
- 使用 JSON 作為內容單一來源，`dist/` 僅為產出物，不得手動編輯。
- 全部站內連結使用相對路徑，支援 GitHub Pages project-site base path。
- 未新增登入、密碼、robots.txt、sitemap、feed、前端 JavaScript 或教材檔案副本。
- 目前仍為示範內容且缺少真人 accessibility 驗收，因此以 ADR-010 防止 push 意外公開。

### 驗證證據

- `npm run ci`：內容驗證通過（2 門課、6 個單元、6 項教材）。
- Node tests：4/4 通過，涵蓋單元結構、Dropbox HTTPS、HTML escaping 與拒絕非 Dropbox 教材 URL。
- Production build：9 個 HTML 頁面。
- 產出頁檢查：noindex、landmarks、標題、內部連結、focus、主要色彩 4.5:1 對比、responsive media query、無 sitemap/feed 均通過。
- `computer-use` 因專案路徑方括號觸發其 sandbox glob 錯誤；Chrome headless GPU 初始化失敗，Edge headless 未產生截圖。因此未宣稱完成視覺、keyboard 或 NVDA 驗收。
- Headless QA 建立的臨時 Edge profile 已移除，沒有停止使用者的 Edge 程序。
- 本機 Git：`main`、`origin` 與初始提交 `cdce7d8` 已存在；目前未確認遠端 `main`、repository 可見性、Pages 設定或 workflow 執行結果。

### 優先待辦

1. 取得正式單元說明、教材 metadata 與 Dropbox 分享連結，逐項替換六個示範項目。
2. 依 `ACCESSIBILITY_TESTING.md` 完成 keyboard-only、NVDA + Chrome 與 320／390／768／1440 px 人工驗收。
3. 對正式頁面執行 axe、Lighthouse Accessibility 與 HTML validator。
4. 確認 GitHub repository 可見性符合需求、遠端 `main` 與 Pages Source 設定。
5. 第一次發布應人工執行 workflow；通過後再決定是否設定 `PUBLISH_PAGES=true`。
